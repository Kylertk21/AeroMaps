<%= form_with(model: @forum_post, local: true) do |form| %>
  <% if forum_post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(forum_post.errors.count, "error") %> prohibited this forum_post from being saved:</h2>

      <ul>
        <% forum_post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :post_topic, style: "display: block", class: "mt-3" %>
    <%= form.text_field :post_topic %>
  </div>

  <div>
    <%= form.label :post_text, style: "display: block" %>
    <%= form.text_area :post_text %>
  </div>

  <div>
    <%= form.submit class: "btn btn-primary ms-3 mt-3" %>
  </div>
<% end %>
